<h1>Users' List</h1>

<div class="row">
  <div class="col-md-6">
    <%= render partial: 'users/list', locals: { users: @first_grouped_users } %>
  </div>
  <div class="col-md-6">
    <% if @second_grouped_users %>
      <%= render partial: 'users/list', locals: { users: @second_grouped_users } %>
    <% end %> 
  </div>
</div>
