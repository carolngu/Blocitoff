<h4>Items</h4>
<% items.each do |it| %>
<div class="item">
  <span><%= it.name %></span>
</div>
<% end %>

<% if new_item %>
  <%= render partial: 'items/form', locals: { item: new_item } %>
<% end %>
